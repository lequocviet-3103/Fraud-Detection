boolean b;
float theta=0;
void setup() { size(600,600); background(0); translate(300,300); }
voiboolean b;
PVector pos, vel, acc;
float mass=20;
void setup() {
  size(600,500);
  pos=newint x;
class Boid {
  PVector pos, vel, acc;
  Boid(float x, float y) {
    pos=new PVector(x,y);
    vfloat y;
class Boid {
  PVector pos, vel, acc;
  Boid(float x, float y) {
    pos=new PVeboolean b;
int[] ca;
int gen=0;
void setup() { size(600,400); ca=new int[width]; ca[width/2]=1; boolean b;
float rx=0, ry=0;
void setup() { size(500,500,P3D); }
void draw() {
  background(20);
  translate(width/2,height/2);
  rotateX(rx); rotateY(rycolor c;
class Particle {
  float x, y, vx, vy, life;
  Particle(float x, float y) {
    this.x=x; this.ycolor c;
PImage img;
void setup() { size(640,480); img=createImage(width,height,RGB); img.loadPixels();
float y;
float[] waves;
float t=0;
void setup() { size(800,400); waves=n