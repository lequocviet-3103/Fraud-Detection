float angle=0;
void setup(){size(500,500);}
void draw(){
  background(220);
  translate(width/String input="";
void setup(){size(600,200);textSize(24);}
void draw(){
  backgro  // reused
int[][] grid;
int rows=20, cols=20, sz;
void setup(){size(600,600); sz=width/cols; grid=new int[rows][cols];}
void d  // reused
int[][] grid;
int rows=20, cols=20, sz;
void setup(){size(600,600); sz=width/cols; grid=new int[rows][cols];}
void d  // reused
2,height/2);
  rotate(angle);
  fill(255,150,50);
  rect(-60,-20,120,40,10);
  angle+=0.02;
}
